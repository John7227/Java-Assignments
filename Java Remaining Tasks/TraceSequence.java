/*Firstly, java was assigned as a value to a variable called name
Then the next line is to calculate the length of the string before it so it calculates how many letters are in the word
And it was finally printed*/

public class TraceSequence {
    public static void main(String[] args) {

	String name = "Java";
	int length = name.length();
	
	System.out.println(name + " has " + length + " letters ");


    }

}