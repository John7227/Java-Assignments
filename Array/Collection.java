import java.util.Arrays;

public class Collection {
	public static int[] collection(int[] number, int[] numbers) {

		int[] collect = new int[item];

		int counter = 0;
		for(int count = 0; count < number.length; count++) {
		    collect[item] = new int[number];
			counter++;
		}

        int counters = 0;
        for(int count = 0; count < numbers.length; count++) {
		    collect[item] = new int[numbers];
            counters++;
        }


//		int value = 0;
//		for(int count = 0; count < number.length; count++) {
//            collect[value] = number[count];
//			value++;
//		}
//
//        int values = 0;
//        for(int count = 0; count < numbers.length; count++) {
//            collect[values] = number[count];
//            values++;
//        }
        return collect;
	}

	public static void main(String[] args) {

	int[] number = {3, 1, 5, 7};

	int[] numbers = {0, 8, 2};

//    int[] value = collection(number, numbers);

//	System.out.println(Arrays.toString(value));
	System.out.print(Arrays.toString(number));
	System.out.println(Arrays.toString(numbers));


	}
}


//	int[] number = decending(numbers);
